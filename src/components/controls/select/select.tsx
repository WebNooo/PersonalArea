import React from 'react';
import {IFormFields} from '../../../interfaces';

export const Select: React.FC<IFormFields> = () => {
  return (
    <select>
      <option>1</option>
      <option>2</option>
    </select>
  );
};
